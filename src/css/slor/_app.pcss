#app {
  display: flex;
  flex-flow: column wrap;
  align-items: center;
  align-content: center;
  margin: -var(--padding-large);

  & > * {
    padding: var(--padding-large);
  }

  @media (--medium-up) {
    flex-direction: row;
  }
}

.demo-headline {
  flex-basis: 100%;
  min-width: 100%;

  @media (--large-up) {
    min-width: unset;
    flex-grow: 1;
    flex-basis: 0;
  }
}

.demo-phone {
  width: 100%;
  /* ViewportHeight / Ratio - app padding * 2 + body padding */
  max-width: calc((100vh / var(--iphone-ratio)) + var(--padding-large) * 2 - var(--padding-giant));
  min-width: 200px;
  flex-basis: 423px;
}

.demo-text {
  @media (--medium-up) {
    flex-grow: 1;
    flex-basis: 0;
  }

  & > div:first-child {
    margin-bottom: var(--padding-large);
  }
}
